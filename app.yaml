runtime: nodejs16

instance_class: F2

env_variables:
  bucket_name: superscan-gcs-bucket

handlers:
  - url: /stylesheets
    static_dir: stylesheets

  - url: /.*
    secure: always
    redirect_http_response_code: 301
    script: auto
