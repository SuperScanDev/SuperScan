runtime: nodejs16

instance_class: F2

env_variables:
  bucket_name: superscan-gcs-bucket
  MONGODB_URI: "mongodb+srv://superscandev:superscandev06@superscan.ntiv5.mongodb.net/SuperScan?retryWrites=true&w=majority"
  PORT: 8080
  PORT_ALTERNATIVE: 5000
  TOKEN_SECRET_KEY: SUPERSCANDEV

handlers:
  - url: /stylesheets
    static_dir: stylesheets
  
  - url: /.*
    secure: always
    redirect_http_response_code: 301
    script: auto
